# 하드웨어(실물 팔) 없이 할 수 있는 작업 목록

> 실물 팔이 아직 미완인 동안, **모터 제어 실기 테스트를 제외하고** 소프트웨어/시뮬레이션 쪽으로
> 진행할 수 있는 작업 모음. 핵심 전략: **"하드웨어 오면 바로 붙을 수 있게 시뮬에서 소프트웨어를
> 미리 완성"** 한다. (작성 2026-07-22)

## 우선순위 요약

- **1순위** — stow 1·2단계 마무리(+방향이면 실물 불필요), YOLO 모델 클래스명 확인
- **2순위** — 물리 시뮬레이터 구축 (나머지 대부분을 실물 없이 검증 가능하게 하는 열쇠)
- **3순위** — 손목 카메라 URDF 통합, SRDF 충돌행렬 정비
- **곁들여** — 파워트레인 계약 위반 2건 수정, 요구사항 모순 정리

---

## 1. 시뮬레이터 환경 구축 ⭐ (레버리지 최대)

- [ ] URDF의 물리 시뮬용 제어 블록(현재 주석 처리된 `ign_ros2_control`) 검토
- [ ] Gazebo 또는 Isaac Sim 중 택1 결정
- [ ] MoveIt mock과 충돌하지 않게 **별도 sim launch**로 분리
- [ ] 시뮬에서 팔이 중력/관성 받으며 명령대로 움직이는지 확인
- [ ] 인식→집기 전체 파이프라인을 시뮬에서 end-to-end 구동

## 2. URDF / MoveIt 다듬기 (실물 0% 필요)

- [ ] **stow 자세 1단계**: RViz(`demo.launch.py`)에서 안전한 접힘 자세 찾기 (+방향 후보)
- [ ] **stow 자세 2단계**: `/check_state_validity`로 self-collision 검증
- [ ] joint_2·3 하한=0 등 관절 리미트 정합성 확인 (실물 음방향 가동 여부는 별도 확인 대기)
- [ ] SRDF 충돌 행렬 정비 (현재 40샘플 18쌍 → 샘플 늘려 근거 강화)
- [ ] 손목 카메라를 URDF **관절로 통합** (현재는 홈 자세 전용 static placeholder)

## 3. 인식(Perception) 개발 — 팔과 독립

- [ ] 학습 모델(`best.pt`)의 **실제 클래스 이름 확인** (막힌 것 하나 뚫기)
- [ ] `pick_classes`/`classes` 파라미터를 실제 클래스명에 맞게 지정
- [ ] `camera_mode:=test`로 인식 파이프라인 로직 개발 (depth 없이 방향추정만)
- [ ] (RealSense 있으면) depth 거리·2D PCA 방향 추정 정확도 실측 — 팔 없어도 가능
- [ ] 새 모델 인식률이 기존 COCO 대비 개선됐는지 실측

## 4. FSM / 제어 로직 (mock 검증)

- [ ] arm_fsm 상태 전이 로직을 mock(`/pick_target`·`/arrival_status`)으로 검증
- [ ] 파워트레인 계약 통합: status 어휘·QoS·DDS 연동을 별도 컨테이너 mock끼리 검증
- [ ] ⚠️ 계약 위반 수정 ①: `DRIVING`에서 팔 언락되는 문제 (주행 중 팔 풀림)
- [ ] ⚠️ 계약 위반 수정 ②: `STOWED_LOCKED`/`CARRYING_LOCKED` 미발행 (stow 자세 확정 후)

## 5. 문서 / 요구사항 정합성

- [ ] `요구사항.md` §8 모순 정리 (DOF 5 vs 6, 모터 XL430 vs XM540, CAN vs UDP 등)
- [ ] 확정된 값으로 CLAUDE.md / WORK_STATUS.md 갱신

---

## 선행조건 있는 것 (실물 필요 → 지금은 보류)

- stow 3단계(실기 저속 검증) — stow 1·2 완료 후
- STOWING / STOW 안전게이트 3건 실기 검증 — stow 자세 확정 후
- 정밀 파지(MoveIt IK) 검증 — URDF 5축 확장 후
- 그리퍼 전류 임계값/스트로크 실측 캘리브 — 실물 그리퍼 조립 후
